# Core (unchanged)
opencv-python>=4.8.0
ultralytics>=8.2.0
deep-sort-realtime>=1.3.2
setuptools<81

# GuardEye Supabase + WebRTC integration (NEW)
# WebRTC video streaming to the GuardEye live monitoring dashboard
aiortc>=1.9.0
websockets>=12.0
# av is a transitive dependency of aiortc but pinning it avoids build issues on Pi
av>=12.0.0
